const fs = require('fs');
let code = fs.readFileSync('src/components/GameCanvas.tsx', 'utf8');

// I'm just going to restore GameCanvas.tsx by checking out from a clean state and manually doing the replace.
